请阅读内容，并只从“现有{{taxonomy_name}}”中选择最匹配的条目。

最多返回 {{count}} 个。不得创造、改写、翻译、合并或纠正现有条目；返回值必须与现有列表完全一致。

现有{{taxonomy_name}}：
{{available}}

内容：
{{content}}

只输出严格 JSON 字符串数组，例如：["条目一","条目二"]。不要输出解释或 Markdown。
