请阅读内容，并同时完成两项任务：
1. 从现有{{taxonomy_name}}中选择最多 {{count}} 个最匹配的条目；必须与现有列表完全一致。
2. 根据内容提出最多 {{count}} 个现有列表中没有的新条目；新条目不得与现有条目同义重复。

现有{{taxonomy_name}}：
{{available}}

内容：
{{content}}

输出要求：
- 只输出严格 JSON 对象，不要解释，不要 Markdown。
- existing 只能包含现有列表中的原文条目。
- new 应简洁、稳定、可复用，避免长句和只适用于单篇内容的临时词。

格式：
{"existing":["已有条目"],"new":["新条目"]}
